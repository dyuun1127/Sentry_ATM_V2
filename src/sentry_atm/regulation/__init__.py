"""규정 엔진 — 「항공교통관제절차」와 AIP 전사값에 근거한 결정론적 판정.

sentry_atm 의 도메인 규칙 계층이다. 분리 최저치·후류·활주로·체공·우선권을 전부
고시 조항과 AIP 전사 데이터에서만 끌어오며 값을 지어내지 않는다.

**외부 의존성이 없다.** 학습(predict, mbe)과 해석적 확률(resolution) 계층만
numpy·scipy·scikit-learn·torch 를 쓰고, 그 임포트는 전부 함수 안에 있다.
따라서 규정 판정 경로는 표준 라이브러리만으로 동작한다 — sentry_atm 본체의
"의존성 0개" 성질을 깨지 않기 위한 배치다.
"""
